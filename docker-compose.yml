services:
  db:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_USER=${POSTGRES_USER:-autopolio}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-autopolio}
      - POSTGRES_DB=${POSTGRES_DB:-autopolio}
    volumes:
      - pgdata:/var/lib/postgresql/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-autopolio}"]
      interval: 5s
      timeout: 5s
      retries: 5

  api:
    build:
      context: .
      dockerfile: Dockerfile.api
    ports:
      - "8085:8000"
    volumes:
      - ./data:/app/data
      - ./result:/app/result
      - ./config:/app/config
      - ./.env:/app/.env.init:ro
      # Mount host CLI auth dirs (read-only — login should happen on host, not in Docker)
      - ~/.claude:/root/.claude:ro
      - ~/.gemini:/root/.gemini:ro
      - ~/.codex:/root/.codex:ro
    env_file:
      - .env
    environment:
      - DATABASE_URL=postgresql+asyncpg://${POSTGRES_USER:-autopolio}:${POSTGRES_PASSWORD:-autopolio}@db:5432/${POSTGRES_DB:-autopolio}
      - ENABLE_MIGRATIONS=true
      - TZ=Asia/Seoul
      - AUTOPOLIO_RUNTIME=docker
    depends_on:
      db:
        condition: service_healthy
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "3035:3000"
    depends_on:
      - api
    restart: unless-stopped

volumes:
  pgdata:
